import React from 'react';
import { Layout, Text, Button } from '@ui-kitten/components';

function Exame () {
    return (
    <Layout style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
      <Text catetory='h1'>Exame Works!</Text>
    </Layout>
    );
}

export default Exame;